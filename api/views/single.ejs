<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Single Posts</title>
    <link rel="stylesheet" href="/css/single.css">
</head>
<body>
    <section class="post-section">
        <header class="post-header">
          <h1 class="post-title"><%= blogs.title %></h1>
          <p class="post-meta"><strong>Author:</strong> <%= blogs.author %></p>
          <p class="post-meta"><small>Created on: <%= blogs.created_at %></small></p>
        </header>
        
        <article class="post-body">
          <p class="post-description"><strong>Description:</strong> <%= blogs.description %></p>
          <p class="post-tags"><strong>Tags:</strong> 
            <% if (Array.isArray(blogs.tags)) { %>
                <%= blogs.tags.join(', ') %>
            <% } else { %>
                <%= blogs.tags %>
            <% } %>
          </p>
          <p class="post-content"><%= blogs.body %></p>
        </article>
        
        <footer class="post-footer">
          <p class="post-state"><strong>State:</strong> <%= blogs.state %></p>
          <a href="/" class="btn back-btn">Back to all posts</a>
        </footer>
      </section>
</body>
</html>